<Page x:Class="EEMC.Views.SwitcherCourseView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:EEMC.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      DataContext="{Binding SwitcherCourseViewVM, Source={StaticResource ViewModelLocator}}"
      Name="SwitcherPage"
      Loaded="Page_Loaded"
      Title="SwitcherCourseView">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.07*"/>
            <RowDefinition Height="0.93*"/>
        </Grid.RowDefinitions>

        <Menu Grid.Row="0">
            <MenuItem Header="Просмотр материалов" Command="{Binding OpenFiles_Click}" FontSize="18"></MenuItem>
            <MenuItem Header="Просмотр занятий" Command="{Binding OpenThemes_Click}" FontSize="18"></MenuItem>
        </Menu>

        <Frame
            Name="CurrentView"
            Content="{Binding CurrentPage}"
            Grid.Row="1"
            NavigationUIVisibility="Hidden"
            Background="#f7f7fa"/>
    </Grid>
</Page>
